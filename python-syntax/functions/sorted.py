# built in global function
# not tied to any specific class, you can call it on any iterable

sorted([3, 1, 2])          # [1, 2, 3]
sorted((3, 1, 2))          # [1, 2, 3]
sorted("cab")              # ['a', 'b', 'c']
sorted({3, 1, 2})          # [1, 2, 3]
